# otobo-texlive-provider
A small, independently versioned Docker image that provisions a full TeX Live installation (including lualatex) into a Docker volume, for use by OTOBO containers that generate PDF documents (e.g. quotes and invoices) via LaTeX.
